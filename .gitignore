node_modules/
dist/
wtms-submission.zip
.DS_Store
.env
.env.local
.vite/
*.log
.vscode/
.idea/
.claude/

# Windows/WSL NTFS stream metadata
*:Zone.Identifier

# AI-generated assets work files (keep only final)
assets/prompts/_scratch/

# Local-only raw asset backups (originals before scripts/strip-bg + compress-music)
assets/raw/

# Visual debugging output (stripped PNGs over a checker for halo inspection)
scripts/preview-out/
